# name: Local Test - CI/CD Next.js

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:

# jobs:
#   build-and-test-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: 🛒 Checkout repository
#         uses: actions/checkout@v4

#       - name: ⚙️ Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: 📦 Install dependencies
#         run: npm ci

#       - name: 🏗️ Build Next.js app
#         run: npm run build

#       - name: 🔍 Verify build output
#         run: |
#           echo "📍 Current directory: $(pwd)"
#           if [ -d ".next" ]; then
#             echo "✅ .next directory exists:"
#             ls -la .next
#           else
#             echo "❌ .next directory not found!"
#             exit 1
#           fi

#       - name: 🗜️ Compress .next folder
#         run: |
#           tar -czf next-build.tar.gz .next
#           echo "✅ Created archive:"
#           ls -lh next-build.tar.gz

#       - name: 🚀 Upload build artifact (for testing)
#         uses: actions/upload-artifact@v4
#         with:
#           name: next-build
#           path: next-build.tar.gz

#       - name: 📦 Simulate "Deploy" locally
#         run: |
#           mkdir -p deploy_output
#           cp next-build.tar.gz deploy_output/
#           cd deploy_output
#           tar -xzf next-build.tar.gz
#           echo "✅ Simulated deployment complete:"
#           ls -la .next

#       - name: ✅ Verify simulated deployment
#         run: |
#           echo "App deployed to local 'deploy_output' folder!"
#           ls -la deploy_output/.next



# name: CI/CD - Next.js Build & Windows Deploy (12:01 AM IST)

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:
#   schedule:
#     - cron: "31 18 * * *" # 12:01 AM IST (18:31 UTC)

# jobs:
#   build-and-deploy:
#     runs-on: windows-latest

#     steps:
#       # ------------------ CI: BUILD ------------------
#       - name: 🛒 Checkout repository
#         uses: actions/checkout@v4

#       - name: ⚙️ Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: 📦 Install dependencies
#         run: npm ci
#         shell: powershell

#       - name: 🏗️ Build Next.js app
#         run: npm run build
#         shell: powershell

#       - name: 🗜️ Create build archive
#         run: |
#           if (Test-Path "next-build.tar.gz") { Remove-Item next-build.tar.gz -Force }
#           tar -czf next-build.tar.gz .next
#           Write-Host "✅ Build archived successfully."
#         shell: powershell

#       # ------------------ CD: DEPLOY ------------------
#       - name: 🚀 Deploy to Windows Server via WinRM
#         shell: pwsh
#         env:
#           SERVER_HOST: ${{ secrets.SERVER_HOST }}
#           SERVER_USER: ${{ secrets.SERVER_USER }}
#           SERVER_PASS: ${{ secrets.SERVER_PASS }}
#         run: |
#           Write-Host "🔧 Configuring WinRM client on GitHub runner..."
#           winrm set winrm/config/client '@{AllowUnencrypted="true"}'
#           winrm set winrm/config/client/auth '@{Basic="true"}'
#           winrm set winrm/config/client '@{TrustedHosts="*"}'
#           Write-Host "✅ WinRM client configured and TrustedHosts set."

#           Write-Host "🔗 Connecting to $env:SERVER_HOST ..."
#           $secpass = ConvertTo-SecureString $env:SERVER_PASS -AsPlainText -Force
#           $creds = New-Object System.Management.Automation.PSCredential ($env:SERVER_USER, $secpass)
#           $session = New-PSSession -ComputerName $env:SERVER_HOST -Port 5985 -Credential $creds -Authentication Basic -UseSSL:$false

#           Write-Host "📦 Uploading build to remote server..."
#           Copy-Item -ToSession $session -Path "next-build.tar.gz" -Destination "C:\inetpub\wwwroot\next-app\next-build.tar.gz" -Force

#           Invoke-Command -Session $session -ScriptBlock {
#             Write-Host "🔧 Preparing deploy folder..."
#             if (!(Test-Path "C:\inetpub\wwwroot\next-app")) {
#               New-Item -ItemType Directory -Path "C:\inetpub\wwwroot\next-app" | Out-Null
#             }

#             Set-Location "C:\inetpub\wwwroot\next-app"
#             Write-Host "📂 Extracting build..."
#             tar -xzf next-build.tar.gz
#             Remove-Item next-build.tar.gz -Force
#             Write-Host "✅ Build extracted successfully."

#             Write-Host "🔄 Restarting Next.js app with PM2..."
#             pm2 restart next-app -f -s 2>$null
#             if ($LASTEXITCODE -ne 0) {
#               Write-Host "App not found — starting a new PM2 process."
#               pm2 start npm --name "next-app" -- run start
#             }

#             # Log deployment timestamp
#             $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
#             Add-Content "C:\inetpub\wwwroot\next-app\deploy-log.txt" "Deployed at $timestamp"
#             Write-Host "🚀 Deployment completed and logged."
#           }

#           Remove-PSSession $session





name: CI/CD - Next.js Build & Windows Deploy (HTTP)

on:
  push:
    branches: ["main"]
  workflow_dispatch:
  schedule:
    - cron: "31 18 * * *" # 12:01 AM IST (18:31 UTC)

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
      # ------------------ CI: BUILD ------------------
      - name: 🛒 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci
        shell: powershell

      - name: 🏗️ Build Next.js app
        run: npm run build
        shell: powershell

      - name: 🗜️ Create build archive
        run: |
          if (Test-Path "next-build.tar.gz") { Remove-Item next-build.tar.gz -Force }
          tar -czf next-build.tar.gz .next package.json
          Write-Host "✅ Build archived successfully."
        shell: powershell

       # ------------------ CD: DEPLOY ------------------
      - name: 🚀 Deploy via WinRM HTTP (Port 5985)
        shell: pwsh
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_PASS: ${{ secrets.SERVER_PASS }}
        run: |
          
          Write-Host "🔧 Configuring WinRM client on GitHub runner..."
          # Set Basic Auth
          winrm set winrm/config/client/auth '@{Basic="true"}'
          # Set TrustedHosts
          winrm set winrm/config/client '@{TrustedHosts="*"}'
          
          # 🚨 CRITICAL FIX: Explicitly allow unencrypted traffic on the client 🚨
          winrm set winrm/config/client '@{AllowUnencrypted="true"}'
          
          Write-Host "✅ WinRM client configured."

          Write-Host "🔗 Connecting to $env:SERVER_HOST on Port 5985 (HTTP)..."

          $secpass = ConvertTo-SecureString $env:SERVER_PASS -AsPlainText -Force
          $creds = New-Object System.Management.Automation.PSCredential ($env:SERVER_USER, $secpass)

          $session = New-PSSession -ComputerName $env:SERVER_HOST -Port 5985 `
            -Credential $creds -Authentication Basic -UseSSL:$false

          Write-Host "📦 Uploading build..."
          Copy-Item -ToSession $session -Path "next-build.tar.gz" `
            -Destination "C:\inetpub\wwwroot\next-app\next-build.tar.gz" -Force

          Invoke-Command -Session $session -ScriptBlock {
            $deployPath = "C:\inetpub\wwwroot\next-app"
            Write-Host "🔧 Preparing deploy folder..."
            if (!(Test-Path $deployPath)) {
              New-Item -ItemType Directory -Path $deployPath | Out-Null
            }

            Set-Location $deployPath
            Write-Host "📂 Extracting build..."
            if (Test-Path ".next") { Remove-Item .next -Recurse -Force }
            if (Test-Path "package.json") { Remove-Item package.json -Force }
            tar -xzf next-build.tar.gz
            Remove-Item next-build.tar.gz -Force

            Write-Host "⚙️ Installing dependencies..."
            npm install --production

            Write-Host "🔄 Restarting app with PM2..."
            pm2 restart next-app -f -s 2>$null
            if ($LASTEXITCODE -ne 0) {
              Write-Host "App not found — starting a new PM2 process."
              pm2 start npm --name "next-app" -- run start
            }

            $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
            Add-Content "$deployPath\deploy-log.txt" "Deployed at $timestamp"
            Write-Host "🚀 Deployment completed and logged."
          }

          Remove-PSSession $session