# name: Local Test - CI/CD Next.js

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:

# jobs:
#   build-and-test-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: 🛒 Checkout repository
#         uses: actions/checkout@v4

#       - name: ⚙️ Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: 📦 Install dependencies
#         run: npm ci

#       - name: 🏗️ Build Next.js app
#         run: npm run build

#       - name: 🔍 Verify build output
#         run: |
#           echo "📍 Current directory: $(pwd)"
#           if [ -d ".next" ]; then
#             echo "✅ .next directory exists:"
#             ls -la .next
#           else
#             echo "❌ .next directory not found!"
#             exit 1
#           fi

#       - name: 🗜️ Compress .next folder
#         run: |
#           tar -czf next-build.tar.gz .next
#           echo "✅ Created archive:"
#           ls -lh next-build.tar.gz

#       - name: 🚀 Upload build artifact (for testing)
#         uses: actions/upload-artifact@v4
#         with:
#           name: next-build
#           path: next-build.tar.gz

#       - name: 📦 Simulate "Deploy" locally
#         run: |
#           mkdir -p deploy_output
#           cp next-build.tar.gz deploy_output/
#           cd deploy_output
#           tar -xzf next-build.tar.gz
#           echo "✅ Simulated deployment complete:"
#           ls -la .next

#       - name: ✅ Verify simulated deployment
#         run: |
#           echo "App deployed to local 'deploy_output' folder!"
#           ls -la deploy_output/.next



# name: CI/CD - Next.js Build & Windows Deploy (12:01 AM IST)

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:
#   schedule:
#     - cron: "31 18 * * *" # 12:01 AM IST (18:31 UTC)

# jobs:
#   build-and-deploy:
#     runs-on: windows-latest

#     steps:
#       # ------------------ CI: BUILD ------------------
#       - name: 🛒 Checkout repository
#         uses: actions/checkout@v4

#       - name: ⚙️ Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: 📦 Install dependencies
#         run: npm ci
#         shell: powershell

#       - name: 🏗️ Build Next.js app
#         run: npm run build
#         shell: powershell

#       - name: 🗜️ Create build archive
#         run: |
#           if (Test-Path "next-build.tar.gz") { Remove-Item next-build.tar.gz -Force }
#           tar -czf next-build.tar.gz .next
#           Write-Host "✅ Build archived successfully."
#         shell: powershell

#       # ------------------ CD: DEPLOY ------------------
#       - name: 🚀 Deploy to Windows Server via WinRM
#         shell: pwsh
#         env:
#           SERVER_HOST: ${{ secrets.SERVER_HOST }}
#           SERVER_USER: ${{ secrets.SERVER_USER }}
#           SERVER_PASS: ${{ secrets.SERVER_PASS }}
#         run: |
#           Write-Host "🔧 Configuring WinRM client on GitHub runner..."
#           winrm set winrm/config/client '@{AllowUnencrypted="true"}'
#           winrm set winrm/config/client/auth '@{Basic="true"}'
#           winrm set winrm/config/client '@{TrustedHosts="*"}'
#           Write-Host "✅ WinRM client configured and TrustedHosts set."

#           Write-Host "🔗 Connecting to $env:SERVER_HOST ..."
#           $secpass = ConvertTo-SecureString $env:SERVER_PASS -AsPlainText -Force
#           $creds = New-Object System.Management.Automation.PSCredential ($env:SERVER_USER, $secpass)
#           $session = New-PSSession -ComputerName $env:SERVER_HOST -Port 5985 -Credential $creds -Authentication Basic -UseSSL:$false

#           Write-Host "📦 Uploading build to remote server..."
#           Copy-Item -ToSession $session -Path "next-build.tar.gz" -Destination "C:\inetpub\wwwroot\next-app\next-build.tar.gz" -Force

#           Invoke-Command -Session $session -ScriptBlock {
#             Write-Host "🔧 Preparing deploy folder..."
#             if (!(Test-Path "C:\inetpub\wwwroot\next-app")) {
#               New-Item -ItemType Directory -Path "C:\inetpub\wwwroot\next-app" | Out-Null
#             }

#             Set-Location "C:\inetpub\wwwroot\next-app"
#             Write-Host "📂 Extracting build..."
#             tar -xzf next-build.tar.gz
#             Remove-Item next-build.tar.gz -Force
#             Write-Host "✅ Build extracted successfully."

#             Write-Host "🔄 Restarting Next.js app with PM2..."
#             pm2 restart next-app -f -s 2>$null
#             if ($LASTEXITCODE -ne 0) {
#               Write-Host "App not found — starting a new PM2 process."
#               pm2 start npm --name "next-app" -- run start
#             }

#             # Log deployment timestamp
#             $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
#             Add-Content "C:\inetpub\wwwroot\next-app\deploy-log.txt" "Deployed at $timestamp"
#             Write-Host "🚀 Deployment completed and logged."
#           }

#           Remove-PSSession $session


# name: CI/CD - Next.js Build & Windows Deploy (HTTP)

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:
#   schedule:
#     - cron: "31 18 * * *" # 12:01 AM IST (18:31 UTC)

# jobs:
#   build-and-deploy:
#     runs-on: windows-latest

#     steps:
#       - name: 🛒 Checkout repository
#         uses: actions/checkout@v4

#       - name: ⚙️ Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: 📦 Install dependencies
#         run: npm ci
#         shell: powershell

#       - name: 🏗️ Build Next.js app
#         run: npm run build
#         shell: powershell

#       - name: 🗜️ Create build archive
#         run: |
#           if (Test-Path "next-build.tar.gz") { Remove-Item next-build.tar.gz -Force }
#           tar -czf next-build.tar.gz .next package.json
#           Write-Host "✅ Build archived successfully."
#         shell: powershell

#       # ------------------ DEPLOY ------------------
#       - name: 🚀 Deploy via WinRM HTTP (Port 5985)
#         shell: pwsh
#         env:
#           SERVER_HOST: ${{ secrets.SERVER_HOST }}
#           SERVER_USER: ${{ secrets.SERVER_USER }}
#           SERVER_PASS: ${{ secrets.SERVER_PASS }}
#         run: |
#           Write-Host "🔧 Configuring WinRM client on GitHub runner..."
#           try {
#             winrm set winrm/config/client/auth '@{Basic="true"}'
#             winrm set winrm/config/client '@{TrustedHosts="*"}'
#             winrm set winrm/config/client '@{AllowUnencrypted="true"}'
#             Write-Host "✅ WinRM client configured."
#           } catch {
#             Write-Host "❌ Failed configuring WinRM client:"
#             Write-Host $_.Exception.Message
#             exit 1
#           }

#           Write-Host "🔗 Connecting to $env:SERVER_HOST on Port 5985..."
#           $secpass = ConvertTo-SecureString $env:SERVER_PASS -AsPlainText -Force
#           $creds = New-Object System.Management.Automation.PSCredential ($env:SERVER_USER, $secpass)

#           $session = $null
#           $maxAttempts = 5
#           for ($i = 1; $i -le $maxAttempts; $i++) {
#             try {
#               Write-Host "🧠 Attempt ${i}: Trying to create PSSession..."
#               $session = New-PSSession -ComputerName $env:SERVER_HOST -Port 5985 `
#                 -Credential $creds -Authentication Basic -UseSSL:$false -ErrorAction Stop
#               Write-Host "✅ Connected successfully on attempt ${i}"
#               break
#             } catch {
#               Write-Host "⚠️ Attempt ${i} failed. Retrying..."
#               Start-Sleep -Seconds 5
#               if ($i -eq $maxAttempts) { throw "❌ Failed to connect after ${maxAttempts} attempts." }
#             }
#           }

#           Write-Host "📦 Uploading build..."
#           Copy-Item -ToSession $session -Path "next-build.tar.gz" `
#             -Destination "C:\inetpub\wwwroot\next-app\next-build.tar.gz" -Force
#           Write-Host "✅ Build uploaded successfully."

#           Write-Host "🧩 Running remote deployment..."
#           Invoke-Command -Session $session -ScriptBlock {
#             $deployPath = "C:\inetpub\wwwroot\next-app"
#             Write-Host "🔧 Preparing deploy folder..."
#             if (!(Test-Path $deployPath)) { New-Item -ItemType Directory -Path $deployPath | Out-Null }
#             Set-Location $deployPath

#             Write-Host "📂 Extracting build..."
#             if (Test-Path ".next") { Remove-Item .next -Recurse -Force }
#             if (Test-Path "package.json") { Remove-Item package.json -Force }
#             tar -xzf next-build.tar.gz
#             Remove-Item next-build.tar.gz -Force

#             Write-Host "⚙️ Installing dependencies..."
#             npm install --omit=dev

#             Write-Host "🛠️ Ensuring PM2 is available..."
#             $pm2Path = 'C:\Users\Administrator\AppData\Roaming\npm'
#             if (!(Test-Path "$pm2Path\pm2.ps1")) {
#               npm install pm2 -g
#               Write-Host "✅ PM2 installed globally."
#             }

#             # Add PM2 to path for this session
#             [System.Environment]::SetEnvironmentVariable("Path", $env:Path + ";$pm2Path", [System.EnvironmentVariableTarget]::Process)
#             $env:Path += ";$pm2Path"
#             Write-Host "✅ PM2 path added: $pm2Path"

#             Write-Host "🔄 Restarting or starting app with PM2..."
#             try {
#               pm2 delete next-app -s 2>$null | Out-Null
#               $rawOutput = pm2 start npm --name "next-app" -- run start --no-warnings | Out-String
#               $cleanOutput = $rawOutput -replace '[^\x20-\x7E]', ''  # strip non-ASCII characters
#               Write-Host $cleanOutput
#               pm2 save | Out-Null
#               Write-Host "✅ PM2 app started and saved successfully."
#             } catch {
#               Write-Host "⚠️ PM2 error:"
#               Write-Host $_.Exception.Message
#             }

#             pm2 list | Out-String | Out-File "$deployPath\pm2-status.txt" -Encoding utf8
#             Write-Host "📄 PM2 status saved to pm2-status.txt"

#             $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
#             Add-Content "$deployPath\deploy-log.txt" "Deployed successfully at $timestamp"
#             Write-Host "🚀 Deployment completed and logged successfully."
#           } -ErrorAction SilentlyContinue

#           Write-Host "✅ Deployment finished cleanly on remote server."
#           Remove-PSSession $session
#           exit 0



# name: CI/CD - Next.js Build & Windows Deploy (HTTP, Multi-Server)

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:
#   schedule:
#     - cron: "31 18 * * *" # 12:01 AM IST (18:31 UTC)

# jobs:
#   build-and-deploy:
#     runs-on: windows-latest

#     # 👇 Matrix defines multiple servers
#     strategy:
#       matrix:
#         include:
#           - name: server1
#             host: ${{ secrets.SERVER1_HOST }}
#             user: ${{ secrets.SERVER1_USER }}
#             pass: ${{ secrets.SERVER1_PASS }}
#           - name: server2
#             host: ${{ secrets.SERVER2_HOST }}
#             user: ${{ secrets.SERVER2_USER }}
#             pass: ${{ secrets.SERVER2_PASS }}

#     name: Deploy to ${{ matrix.name }}

#     steps:
#       # ------------------ CI: BUILD ------------------
#       - name: 🛒 Checkout repository
#         uses: actions/checkout@v4

#       - name: ⚙️ Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: 📦 Install dependencies
#         run: npm ci
#         shell: powershell

#       - name: 🏗️ Build Next.js app
#         run: npm run build
#         shell: powershell

#       - name: 🗜️ Create build archive
#         run: |
#           if (Test-Path "next-build.tar.gz") { Remove-Item next-build.tar.gz -Force }
#           tar -czf next-build.tar.gz .next package.json
#           Write-Host "✅ Build archived successfully."
#         shell: powershell

#       # ------------------ CD: DEPLOY ------------------
#       - name: 🚀 Deploy to ${{ matrix.name }} via WinRM HTTP
#         shell: pwsh
#         env:
#           SERVER_HOST: ${{ matrix.host }}
#           SERVER_USER: ${{ matrix.user }}
#           SERVER_PASS: ${{ matrix.pass }}
#         run: |
#           Write-Host "🔧 Configuring WinRM client..."
#           try {
#             winrm set winrm/config/client/auth '@{Basic="true"}'
#             winrm set winrm/config/client '@{TrustedHosts="*"}'
#             winrm set winrm/config/client '@{AllowUnencrypted="true"}'
#             Write-Host "✅ WinRM client configured."
#           } catch {
#             Write-Host "❌ Failed configuring WinRM client:"; Write-Host $_.Exception.Message; exit 1
#           }

#           Write-Host "🔗 Connecting to $env:SERVER_HOST ..."
#           $secpass = ConvertTo-SecureString $env:SERVER_PASS -AsPlainText -Force
#           $creds = New-Object System.Management.Automation.PSCredential ($env:SERVER_USER, $secpass)

#           $session = $null
#           $maxAttempts = 5
#           for ($i = 1; $i -le $maxAttempts; $i++) {
#             try {
#               Write-Host "🧠 Attempt ${i}: Trying to create PSSession..."
#               $session = New-PSSession -ComputerName $env:SERVER_HOST -Port 5985 `
#                 -Credential $creds -Authentication Basic -UseSSL:$false -ErrorAction Stop
#               Write-Host "✅ Connected successfully on attempt ${i}"
#               break
#             } catch {
#               Write-Host "⚠️ Attempt ${i} failed: $($_.Exception.Message)"
#               Start-Sleep -Seconds 5
#               if ($i -eq $maxAttempts) { throw "❌ Failed after ${maxAttempts} attempts." }
#             }
#           }

#           Write-Host "📦 Uploading build to $env:SERVER_HOST..."
#           Copy-Item -ToSession $session -Path "next-build.tar.gz" `
#             -Destination "C:\inetpub\wwwroot\next-app\next-build.tar.gz" -Force

#           Write-Host "🧩 Running remote deployment on $env:SERVER_HOST..."
#           Invoke-Command -Session $session -ScriptBlock {
#             $deployPath = "C:\inetpub\wwwroot\next-app"
#             Write-Host "🔧 Preparing deploy folder..."
#             if (!(Test-Path $deployPath)) { New-Item -ItemType Directory -Path $deployPath | Out-Null }
#             Set-Location $deployPath

#             Write-Host "📂 Extracting build..."
#             if (Test-Path ".next") { Remove-Item .next -Recurse -Force }
#             if (Test-Path "package.json") { Remove-Item package.json -Force }
#             tar -xzf next-build.tar.gz
#             Remove-Item next-build.tar.gz -Force

#             Write-Host "⚙️ Installing dependencies..."
#             npm install --omit=dev

#             Write-Host "🛠️ Ensuring PM2 is available..."
#             $pm2Path = 'C:\Users\Administrator\AppData\Roaming\npm'
#             if (!(Test-Path "$pm2Path\pm2.ps1")) {
#               npm install pm2 -g
#               Write-Host "✅ PM2 installed globally."
#             }

#             # Add PM2 to path for this session
#             [System.Environment]::SetEnvironmentVariable("Path", $env:Path + ";$pm2Path", [System.EnvironmentVariableTarget]::Process)
#             $env:Path += ";$pm2Path"
#             Write-Host "✅ PM2 path added: $pm2Path"

#             Write-Host "🔄 Restarting or starting app with PM2..."
#             try {
#               pm2 delete next-app -s 2>$null | Out-Null
#               $rawOutput = pm2 start npm --name "next-app" -- run start --no-warnings | Out-String
#               $cleanOutput = $rawOutput -replace '[^\x20-\x7E]', ''
#               Write-Host $cleanOutput
#               pm2 save | Out-Null
#               Write-Host "✅ PM2 app started and saved successfully."
#             } catch {
#               Write-Host "⚠️ PM2 error: $($_.Exception.Message)"
#             }

#             pm2 list | Out-String | Out-File "$deployPath\pm2-status.txt" -Encoding utf8
#             $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
#             Add-Content "$deployPath\deploy-log.txt" "Deployed successfully at $timestamp"
#             Write-Host "🚀 Deployment completed and logged successfully."
#           } -ErrorAction SilentlyContinue

#           Write-Host "✅ Deployment finished cleanly on $env:SERVER_HOST."
#           Remove-PSSession $session
#           exit 0


name: CI/CD - Next.js Build & Windows Deploy (Multi-Server Stable)

on:
  push:
    branches: ["main"]
  workflow_dispatch:
  schedule:
    - cron: "31 18 * * *" # 12:01 AM IST (18:31 UTC)

jobs:
  build-and-deploy:
    runs-on: windows-latest

    strategy:
      matrix:
        include:
          - name: server1
            server: SERVER1
          - name: server2
            server: SERVER2

    name: Deploy to ${{ matrix.name }}

    steps:
      # ---------------- CI ----------------
      - name: 🛒 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci
        shell: powershell

      - name: 🏗️ Build Next.js
        run: |
          Write-Host "🏗️ Building Next.js app..."
          npx next telemetry disable
          npm run build
        shell: powershell

      - name: 🗜️ Create build archive (safe with optional files)
        run: |
          Write-Host "🗜️ Creating build archive safely..."
          if (Test-Path "next-build.tar.gz") { Remove-Item next-build.tar.gz -Force }

          if (-not (Test-Path ".next")) {
            Write-Host "❌ Error: .next folder not found. Build may have failed."
            exit 1
          }
          if (-not (Test-Path "package.json")) {
            Write-Host "❌ Error: package.json missing!"
            exit 1
          }

          $tarItems = @(".next", "package.json")
          if (Test-Path "public") { $tarItems += "public" }
          if (Test-Path "next.config.js") { $tarItems += "next.config.js" }

          Write-Host "🧩 Archiving items: $($tarItems -join ', ')"
          tar -czf next-build.tar.gz @tarItems
          Write-Host "✅ Build archive created successfully."
        shell: powershell

      # ---------------- CD ----------------
      - name: 🚀 Deploy via WinRM HTTP
        shell: pwsh
        env:
          SERVER1_HOST: ${{ secrets.SERVER1_HOST }}
          SERVER1_USER: ${{ secrets.SERVER1_USER }}
          SERVER1_PASS: ${{ secrets.SERVER1_PASS }}
          SERVER2_HOST: ${{ secrets.SERVER2_HOST }}
          SERVER2_USER: ${{ secrets.SERVER2_USER }}
          SERVER2_PASS: ${{ secrets.SERVER2_PASS }}
        run: |
          # 🧩 Pick correct server credentials dynamically
          switch ("${{ matrix.server }}") {
            "SERVER1" {
              $env:SERVER_HOST = "${{ secrets.SERVER1_HOST }}"
              $env:SERVER_USER = "${{ secrets.SERVER1_USER }}"
              $env:SERVER_PASS = "${{ secrets.SERVER1_PASS }}"
            }
            "SERVER2" {
              $env:SERVER_HOST = "${{ secrets.SERVER2_HOST }}"
              $env:SERVER_USER = "${{ secrets.SERVER2_USER }}"
              $env:SERVER_PASS = "${{ secrets.SERVER2_PASS }}"
            }
          }

          Write-Host "🔧 Configuring WinRM client..."
          try {
            winrm set winrm/config/client/auth '@{Basic="true"}'
            winrm set winrm/config/client '@{TrustedHosts="*"}'
            winrm set winrm/config/client '@{AllowUnencrypted="true"}'
            Write-Host "✅ WinRM client configured successfully."
          } catch {
            Write-Host "❌ Failed configuring WinRM client: $($_.Exception.Message)"
            exit 1
          }

          Write-Host "🔗 Connecting to $env:SERVER_HOST..."
          $secpass = ConvertTo-SecureString $env:SERVER_PASS -AsPlainText -Force
          $creds = New-Object System.Management.Automation.PSCredential ($env:SERVER_USER, $secpass)

          $session = $null
          $maxAttempts = 5
          for ($i = 1; $i -le $maxAttempts; $i++) {
            try {
              Write-Host "🧠 Attempt ${i}: Creating PSSession..."
              $session = New-PSSession -ComputerName $env:SERVER_HOST -Port 5985 `
                -Credential $creds -Authentication Basic -UseSSL:$false -ErrorAction Stop
              Write-Host "✅ Connected successfully."
              break
            } catch {
              Write-Host "⚠️ Connection failed (${i}): $($_.Exception.Message)"
              Start-Sleep -Seconds 5
              if ($i -eq $maxAttempts) { throw "❌ Failed after ${maxAttempts} attempts." }
            }
          }

          Write-Host "📦 Uploading build..."
          Copy-Item -ToSession $session -Path "next-build.tar.gz" `
            -Destination "C:\inetpub\wwwroot\next-app\next-build.tar.gz" -Force
          Write-Host "✅ Build uploaded successfully."

          Write-Host "🧩 Deploying on $env:SERVER_HOST..."
          Invoke-Command -Session $session -ScriptBlock {
            $deployPath = "C:\inetpub\wwwroot\next-app"
            $backupPath = "$deployPath\backup_latest"
            Set-Location $deployPath

            Write-Host "📦 Backing up current build..."
            if (Test-Path $backupPath) { Remove-Item $backupPath -Recurse -Force }
            New-Item -ItemType Directory -Path $backupPath | Out-Null
            if (Test-Path ".next") { Copy-Item ".next" -Destination $backupPath -Recurse }
            if (Test-Path "package.json") { Copy-Item "package.json" -Destination $backupPath }
            if (Test-Path "public") { Copy-Item "public" -Destination $backupPath -Recurse }

            Write-Host "📂 Extracting new build..."
            try {
              if (Test-Path ".next") { Remove-Item .next -Recurse -Force }
              if (Test-Path "package.json") { Remove-Item package.json -Force }
              if (Test-Path "public") { Remove-Item public -Recurse -Force }

              tar -xzf next-build.tar.gz
              Remove-Item next-build.tar.gz -Force

              Write-Host "⚙️ Installing dependencies..."
              npm install --omit=dev

              # PM2 setup
              $pm2Path = 'C:\Users\Administrator\AppData\Roaming\npm'
              [System.Environment]::SetEnvironmentVariable("Path", $env:Path + ";$pm2Path", [System.EnvironmentVariableTarget]::Process)
              $env:Path += ";$pm2Path"
              if (!(Test-Path "$pm2Path\pm2.ps1")) { npm install pm2 -g }

              Write-Host "🔄 Restarting app..."
              pm2 restart next-app -f -s 2>$null
              Start-Sleep -Seconds 8
              $status = (pm2 info next-app | Out-String)
              if ($status -match "online") {
                Write-Host "✅ New build is running fine!"
              } else {
                throw "❌ New build failed to start."
              }

            } catch {
              Write-Host "⚠️ Deployment failed — restoring backup..."
              if (Test-Path ".next") { Remove-Item .next -Recurse -Force }
              if (Test-Path "package.json") { Remove-Item package.json -Force }
              if (Test-Path "public") { Remove-Item public -Recurse -Force }
              Copy-Item "$backupPath\*" -Destination $deployPath -Recurse -Force
              pm2 restart next-app -f -s 2>$null
              Write-Host "♻️ Reverted to previous working version."
            }

            pm2 save | Out-Null
            pm2 list | Out-String | Out-File "$deployPath\pm2-status.txt" -Encoding utf8
            Add-Content "$deployPath\deploy-log.txt" "Deployed at $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
            Write-Host "✅ Deployment finished successfully."
          }

          Remove-PSSession $session
          Write-Host "✅ Done with $env:SERVER_HOST."
